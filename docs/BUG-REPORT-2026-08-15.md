    # Invora — Bug Report (15 Aug 2026)

Target: https://invorateam12.vercel.app/ · Repo `D:\vertex saas` @ `53009d7` (main)

**How it was tested**
- Live site driven in a browser (desktop 1280px + mobile 375px): every public page, every public API route (unauthenticated probes only — no accounts created, no forms submitted, no payments), response headers, metadata, console/network.
- Full read of the codebase by three parallel reviewers (marketing/auth/public · app pages/documents/AI · payments/admin/cron/DB migrations), with each finding checked against the actual code path.
- `tsc --noEmit`, `next lint`, `vitest run` — all clean (93 tests pass; RLS integration suite skipped because it needs Supabase env).

**Totals:** 4 production-config blockers · 5 critical/high security · ~14 high correctness · ~45 medium · ~55 low/trivial. Severity key: 🔴 Critical · 🟠 High · 🟡 Medium · 🔵 Low · ⚪ Trivial.

---

## A. Production configuration blockers (verified live)

These are not code bugs so much as the deployment being missing environment variables — but they make most of the product non-functional right now.

| # | Sev | Finding | Evidence | Fix |
|---|---|---|---|---|
| A1 | 🔴 | **Rate limiter fails closed → HTTP 429 on almost every API.** `UPSTASH_REDIS_REST_URL/TOKEN` are not set in prod, and `lib/guards/rate-limit.ts:56-61` throws 429 in production when no Redis is configured. | Live: `/api/meetings/availability`, `/api/meetings/book`, `/api/pdf/...`, `/api/share/respond`, `/api/ai/support-chat` all return `429 rate_limited` on the very first request. | Set the two Upstash vars in Vercel. Affects: demo booking, contact form, support chat, quote accept/decline, PDF/Excel download, sending documents by email, all in-app AI, Razorpay order/verify, manual payments, share-link creation, subscription checkout, report export, CSV import. Only login/signup skip the limiter (`app/(auth)/actions.ts:46`). |
| A2 | 🔴 | **`NEXT_PUBLIC_APP_URL` is blank in prod.** Every fallback resolves to `http://localhost:3000`. | Live: `<link rel=canonical href="http://localhost:3000/">`, `og:url`, `og:image`, and every `<loc>` in `/sitemap.xml` are localhost. | Set it to `https://invorateam12.vercel.app` (no trailing slash). Consequences while blank: (a) **sign-up and forgot-password crash** — `originUrl()` in `app/(auth)/actions.ts:31-37` throws in production when the var is empty, before `supabase.auth.signUp` runs; (b) share links (`lib/share/tokens.ts:72`), receipt/decision emails (`app/api/webhooks/razorpay/route.ts:238`, `app/api/share/respond/route.ts:103`) point at localhost; (c) SEO canonical/sitemap/OG all wrong; (d) Google Calendar connect throws (`lib/google/calendar.ts:24-27`). |
| A3 | 🔴 | **`CRON_SECRET` not set → every cron returns 403.** | Live: `GET /api/cron/overdue` → `403 "Cron is not configured."` (`lib/guards/cron.ts:16`). | Set `CRON_SECRET` in Vercel (and the same value as a GitHub secret for the demo-reminders workflow). Until then invoices never go overdue, quotes never expire, reminders never send, free-plan usage never resets, demo Meet links never go out. |
| A4 | 🔴 | **`RAZORPAY_WEBHOOK_SECRET` not set → webhook returns 500.** | Live: `POST /api/webhooks/razorpay` → `500 {"error":"not_configured"}` (`route.ts:32-36`). | Set the secret. Until then no online payment can ever mark an invoice paid and no subscription can activate. |
| A5 | 🟡 | Booking UI hides API failures as "No open slots that day". | `app/(marketing)/book-demo/book-demo-flow.tsx:163-170` — any non-2xx / 429 → `setSlots([])`. Live: the page says "No open slots" for every date because of A1. | Check `response.ok`, show "Couldn't load availability — try again" state. |
| A6 | 🔵 | `/favicon.ico` → 404. Only `/icon.png` exists; middleware matcher even special-cases favicon.ico. | Live: `GET /favicon.ico` 404. | Add `app/favicon.ico` (or `public/favicon.ico`). |
| A7 | 🔵 | `vercel.json` declares 4 cron jobs while the repo says the project is on Vercel Hobby (max 2 crons, daily) — `.github/workflows/demo-reminders.yml:3-4`, commit `0379f4f`. | Either extra crons don't fire, or the "Hobby" comments are stale. | Consolidate to ≤2 endpoints (`/api/cron/nightly` running overdue+expire+usage-reset) or move to GitHub Actions like demo-reminders. |

---

## B. Security & tenant isolation

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| B1 | 🔴 | **Quota RPCs are `SECURITY DEFINER` with no ownership check.** `consume_ai_credits`, `release_ai_credits`, `consume_document_quota`, `ensure_usage_period`, `effective_limits` take an arbitrary `p_business_id`, never compare to `auth.uid()`, and are not `REVOKE`d from `authenticated` — so PostgREST exposes them. Any signed-in user can `rpc('release_ai_credits', {p_business_id: mine, p_amount: 100000})` → unlimited free AI, or `consume_document_quota(victim, 500)` → burn another tenant's allowance (business ids appear in public logo URLs). | `supabase/migrations/0005_functions_and_triggers.sql:258-336` (contrast `next_document_number:122` and `convert_quotation_to_invoice:359`, which do check) | Add the `current_business_id()` guard to all five; `revoke execute ... from anon, authenticated` on the ones only the service role calls. Add RLS-test coverage. |
| B2 | 🟠 | **Reporting views leak every tenant's revenue/receivables/MRR.** Views are created without `security_invoker = true`, so they run as owner and bypass RLS; only `anon` is revoked, `authenticated` keeps SELECT. Any signed-in user can `GET /rest/v1/v_revenue_daily`, `v_receivables`, `v_mrr_by_plan`, `v_ai_cost_daily`. | `supabase/migrations/0008_admin_views.sql:8-66` (comment at :62 is wrong for Supabase) | `alter view ... set (security_invoker = true)` and revoke from `authenticated`. |
| B3 | 🟠 | **Suspended users can un-suspend themselves.** `app_users self update` only protects `role`; `suspended_at` (and `email`) are owner-writable via `PATCH /rest/v1/app_users`. | `supabase/migrations/0006_rls_policies.sql:37-39`; enforcement `lib/guards/auth.ts:83-86` | Lock `suspended_at` in WITH CHECK, or column-level grant on `full_name` only. |
| B4 | 🟡 | **Sent/viewed/accepted/paid documents are editable server-side.** `readOnly` in the UI only covers accepted/rejected/expired quotes and invoices with payments; `saveQuotation/saveInvoice` never check `status`, so an already-emailed quotation (with a live share link) can have amounts changed silently, and even paid/cancelled docs can be rewritten by a crafted action call. | `app/(app)/quotations/[id]/page.tsx:48`, `app/(app)/invoices/[id]/page.tsx:47`, `lib/documents/service.ts:133-162,248-275` | Reject updates when status ∉ draft (or force back to draft + timeline "edited" event). |
| B5 | 🟡 | `/api/share/respond` accepts responses to **draft** quotations and has a check-then-update race (two concurrent submits both pass). | `app/api/share/respond/route.ts:54-73` | `.update().eq('id').in('status',['sent','viewed'])`, treat 0 rows as conflict. |
| B6 | 🟡 | PostgREST filter injection / breakage via `.or()` string interpolation. A comma or parenthesis in the search ("Sharma, Rahul", "Acme (India)") makes customers list show "could not load" and search show "Nothing found"; `%`/`_` unescaped. Same in admin search. | `app/(app)/customers/page.tsx:31`, `app/(app)/search/page.tsx:45-51`, `app/api/admin/businesses/route.ts:31-33` | Escape `,()%_` or use separate `.ilike()` calls / RPC. |
| B7 | 🟡 | Profile form round-trips `logo_url`/`signature_url` as free text — any URL ≤500 chars accepted, later fetched server-side by the PDF renderer and rendered in `<img>` on public pages; stale hidden value overwrites a logo uploaded in another tab. Replaced/removed logo objects are never deleted; public bucket accepts `image/svg+xml`. | `components/app/settings-forms.tsx:121-122`, `lib/validation/schemas.ts:79-80`, `components/app/logo-uploader.tsx:87-95,120`, `0006_rls_policies.sql:122` | Don't accept these from the form; restrict to own storage bucket URLs. |
| B8 | 🟡 | `subscriptions.status` is never enforced. Admin "Suspend" (`halted`), Razorpay `past_due`/`pending` leave premium features fully usable; admin "Cancel" only sets `cancel_at_period_end`, which nothing acts on for non-Razorpay subs. | `lib/guards/features.ts:27-51`, `lib/guards/quota.ts`, `app/api/admin/businesses/route.ts:112-114` | Gate on status as well as plan_code. |
| B9 | 🟡 | Convert-to-invoice creates an invoice without consuming document quota and without a server-side status check (any quotation status convertible). | `app/(app)/actions.ts:227-251` vs `duplicateDocumentAction:268-273` | Meter + check status. |
| B10 | 🔵 | Admin login rate limit fails *open* without Redis (everything else fails closed). | `app/(admin)/admin/login/actions.ts:37-45` | Be consistent. |
| B11 | 🔵 | Client-side error reporting is a no-op: `app/error.tsx:22` calls `Sentry.captureException` but there is no `instrumentation-client.ts` / `sentry.client.config.ts` and no `withSentryConfig`; the page still says "The error has been reported automatically." | `app/error.tsx`, `instrumentation.ts`, `next.config.mjs` | Add `instrumentation-client.ts` with `Sentry.init`. |
| B12 | 🔵 | Welcome email is sent before verification and on duplicate sign-ups — anyone can trigger a "Welcome to Invora — your workspace for <arbitrary business name> is ready" mail to any address (Supabase returns no error for an existing address when confirm-email is on). | `app/(auth)/actions.ts:98-113` | Send from the callback after verification; skip when `data.user.identities.length === 0`. |
| B13 | 🔵 | Share links: first view is recorded by link-preview bots (Outlook Safe Links, WhatsApp/Gmail previewers) → `sent → viewed` flips before the human opens it; `view_count` is a non-atomic read-modify-write. | `lib/share/public-document.ts:163-190` | Ignore HEAD/known bot UAs; increment via SQL. |

---

## C. Money, numbering, documents & data correctness

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| C1 | 🟠 | **Document numbering truncates and collides once the counter outgrows `number_padding`.** Postgres `lpad('10000', 4, '0')` = `'1000'` (it truncates), so invoice #10000 → `INV-1000` (already used) → unique violation → *every* new document / duplicate / conversion fails forever. `number_padding` can be set as low as 1 (`lib/validation/schemas.ts:87`), so padding 1 breaks at the 10th document, padding 2 at the 100th. | `supabase/migrations/0005_functions_and_triggers.sql:145` | `lpad(v_no::text, greatest(v_pad, length(v_no::text)), '0')`. |
| C2 | 🟠 | **PDFs can't render `₹`, `−`, or any Indic script.** Only base-14 Helvetica is used (no `Font.register` anywhere); `formatPaise` emits `₹1,234.00` and discount/paid rows use U+2212. WinAnsi fonts print these as garbage/blank — every currency figure on every PDF is corrupted, and the Hindi/Marathi/Gujarati/Tamil/Telugu/Bengali options in the AI dialog produce blank text in the PDF. | `lib/pdf/shared.tsx:22-27,152,197`, `lib/money.ts:8`, `components/documents/ai-generate-dialog.tsx:166` | Register a Unicode TTF (Noto Sans + Noto Sans Devanagari etc.) or at minimum use `Rs.` and ASCII `-`. |
| C3 | 🟠 | **Autosave has no in-flight guard → duplicate documents, duplicate numbers, double quota.** A new document's first save (`docId === null`) can take >2 s on a cold function; another keystroke or clicking Save fires a second `docId === null` save → second row, second number, second quota unit. | `components/documents/document-editor.tsx:79-122` | Track an in-flight promise; queue saves until `docId` is known. |
| C4 | 🟠 | **`replaceItems` is not concurrency-safe — two overlapping saves can delete all line items** (A inserts a, B inserts b, A deletes ∉a, B deletes ∉b → zero items, stale totals). Reachable via C3. | `lib/documents/service.ts:295-347` | Serialize per document (advisory lock / single transactional RPC). |
| C5 | 🟠 | **Webhook: a failed event can never be retried.** The `webhook_events` row is inserted before processing; on failure it's marked `failed` and 500 returned "so Razorpay retries" — but the retry hits the UNIQUE violation and returns `200 duplicate_ignored`. A transient blip during `payment.captured` = a paid invoice never credited. | `app/api/webhooks/razorpay/route.ts:57-71,110-116` | On 23505, re-run when the existing row is `failed`; only short-circuit on `processed/ignored`. |
| C6 | 🟠 | **Subscriptions not `active`+`free` are never rolled to a new period → permanent quota lockout** ("You have created all 10 documents…" with 0 used) for downgraded (`cancelled/expired`), admin-comped premium, abandoned-checkout (`pending`), and `past_due` users. `ensure_usage_period` recreates the same expired row. | `app/api/cron/usage-reset/route.ts:26-31`, `webhooks/razorpay/route.ts:349-363`, `admin/businesses/route.ts:104-108`, `subscription/checkout/route.ts:58-64`, `0005:319-324` | Roll on `plan_code='free'` regardless of status; make `ensure_usage_period` roll lazily as its comment claims. |
| C7 | 🟡 | Every free-plan business is locked out between period end (00:00 UTC = 05:30 IST) and the daily cron (02:10 UTC), and for a full day if the cron fails/there are >500 rows. | `0005:83-89`, `usage-reset/route.ts:11` (comment still says "hourly"), `vercel.json:18-19` | Lazy roll inside `ensure_usage_period`. |
| C8 | 🟡 | Yearly Premium gets 500 docs / 500 credits **per year**, not per month — usage period is set to Razorpay's 12-month billing cycle and `usage-reset` skips paid plans. | `webhooks/razorpay/route.ts:303-346`, `0007_seed_plans.sql:39-42` | Derive monthly usage periods independently of billing cycle. |
| C9 | 🟡 | `recalc_invoice_payment_state` sums payments *before* `FOR UPDATE` → concurrent inserts (manual + Razorpay / double-click) write a stale `amount_paid_paise`. Manual-payment route also checks balance non-atomically (`payments/manual/route.ts:38-46`) → overpayment. | `0005:166-169` | Lock first, then sum; enforce balance in SQL. |
| C10 | 🟡 | Stale Razorpay order can over-pay: order amount fixed at creation; a manual payment recorded before checkout completes → captured amount > balance → negative `balance_paise`, no CHECK. | `payments/order/route.ts:85-95`, `webhooks/razorpay/route.ts:174-209` | Compare `payment.amount` to current balance; flag/email on overpay. |
| C11 | 🟡 | Deleting a draft (or a failed insert) permanently burns a number → gaps in the invoice sequence (GST expects consecutive numbering) just by opening `/invoices/new`, typing one letter and leaving. | `app/(app)/actions.ts:395-411`, `lib/documents/service.ts:85-90,201-206` | Assign numbers on first send/finalize, or disallow deleting numbered drafts. |
| C12 | 🟡 | Editor totals ≠ saved totals when a line has amounts but no name: on-screen total includes it, `toPayload` drops it → PDF/list show a different number. | `document-editor.tsx:62-75` vs `components/documents/types.ts:81-82` | Same filter for both, or block save. |
| C13 | 🟡 | AI command bar `remove_line_item` re-keys by array index → rows after the removed one inherit the previous row's `product_id`, `hsn_sac`, description and React key → wrong HSN on PDF. Also the preview is applied on top of possibly-changed lines (user edits while preview open are overwritten). | `components/documents/ai-command-bar.tsx:116-137` | Stable per-line ids; invalidate preview on change. |
| C14 | 🟡 | Customer "Paid to date" is computed from the last 50 payments of the *whole business*, filtered client-side. | `app/(app)/customers/[id]/page.tsx:41-59` | `.in('invoice_id', invoiceIds)`. |
| C15 | 🟡 | Downloadable report totals include drafts and cancelled invoices; the Reports page excludes drafts, so the two disagree. | `lib/excel/render-report.ts:129-130` vs `app/(app)/reports/page.tsx:52-56` | Filter by status. |
| C16 | 🟡 | Server-rendered dates/times are formatted in the server's zone (UTC on Vercel), not IST — timeline "Sent 14:30" is 5h30 off, a payment at 01:00 IST on the 16th lists as the 15th, admin demo times are 5.5 h early vs the email, receipt `paidOn`. | `lib/utils.ts:9-27`; used in `components/documents/timeline.tsx:64`, `invoices/[id]/page.tsx`, `customers/[id]/page.tsx:155`, `admin/meetings/page.tsx:176`, `payments/page.tsx:226`, `webhooks/razorpay/route.ts:246` | Pass `timeZone: business.timezone ?? 'Asia/Kolkata'`. |
| C17 | 🟡 | Clearing email/phone/GSTIN/PAN/IFSC on an existing customer/business is silently ignored (`''` → `undefined` → key dropped from `.update()`); "Saved." lies. | `lib/validation/common.ts:24-59`, `app/(app)/actions.ts:76-80,423` | Transform `''` → `null` for updates. |
| C18 | 🟡 | Sending a document revokes the customer's existing link *before* the email is known to succeed; if Resend/SMTP fails the old link is dead and no new one delivered. Same in the reminders cron (revokes valid link on every reminder). | `app/api/documents/send/route.ts:65-79,130-135`, `app/api/cron/reminders/route.ts:165-170` | Revoke only after `result.sent`, or allow multiple live links. |
| C19 | 🟡 | Reminders cron: unordered `.limit(500)` with no due-date pre-filter → beyond ~500 open invoices some are never reminded. | `app/api/cron/reminders/route.ts:36-44` | Filter due_date to ladder dates in SQL, paginate. |
| C20 | 🟡 | Long free-text sections (terms/scope up to 8000 chars) are clipped in the PDF — `wrap={false}` on `LongFormSection`. | `lib/pdf/shared.tsx:168,214,227` | Allow wrapping; keep `minPresenceAhead` for headings. |
| C21 | 🟡 | Excel export omits line-item discount % → Amount ≠ Qty×Rate with no explanation; totals labels are written into the width-8 "Tax %" column and truncate. | `lib/excel/render.ts:66-77,104-121` | Add "Disc %" column; widen. |
| C22 | 🟡 | Record-payment dialog keeps a stale default amount after a partial payment (initialised once from `balancePaise`; not reset on reopen) → "more than the outstanding balance", button disabled. | `components/documents/record-payment-dialog.tsx:48` | Reset on open. |
| C23 | 🟡 | Duplicating a document: line-item insert error unchecked (duplicate with totals but zero lines); `issue_date`/`due_date`/`valid_until` copied verbatim (already-expired duplicate); duplicated invoices keep source `quotation_id`; quota consumed before source is confirmed to exist. | `app/(app)/actions.ts:269-342` | Check insert, reset dates, null `quotation_id`, meter after load. |
| C24 | 🟡 | Edits made while a save is in flight are marked "All changes saved" (`setDirty(false)` after the request even if state changed) → `beforeunload` no longer warns; newest edits sit unsaved. | `document-editor.tsx:99-101` | Compare a save-sequence counter. |
| C25 | 🟡 | CSV import rejects the whole file when any row has an extra column (`__parsed_extra` fails `z.record(z.string(), z.string())`) → "payload was malformed" instead of a per-row error. | `app/api/customers/import/route.ts:16`, `components/app/csv-importer.tsx:61-63` | Strip `__parsed_extra`. |
| C26 | 🟡 | "Draft with AI" for a quotation cover message uses `kind: 'reminder'` → reminder-flavoured copy ("sent before the due date") for a first-time quotation email. | `components/documents/send-dialog.tsx:58`, `app/api/ai/invoice/route.ts:60-66` | Add a neutral `cover_message` kind. |
| C27 | 🟡 | Webhook silently keeps `plan_code='free'` when the Razorpay plan-id env var is missing/mismatched → paying customer stays free with `status='active'`. | `webhooks/razorpay/route.ts:301,325` | Fail the event loudly. |
| C28 | 🟡 | Checkout POST clobbers the existing subscription row (`razorpay_subscription_id`, `status='pending'`) before any payment → active premium user switching plans loses the link to their live sub; abandoned free user left `pending` (→ C6). | `app/api/subscription/checkout/route.ts:57-64` | Store pending id separately / write on `subscription.activated`. |
| C29 | 🟡 | Reconcile flags every subscription charge as a "missing payment" forever (compares all `payment.captured` events incl. subscription charges against `payments`); reads untrusted `payment.notes.invoice_id`. | `app/api/admin/reconcile/route.ts:57-72` | Skip events whose order has no `invoice_id`. |
| C30 | 🟡 | Admin "Active businesses" KPI counts document *events* in 7 days, not distinct businesses. | `app/api/admin/stats/route.ts:42-47`, `components/admin/overview.tsx:188` | `count(distinct business_id)`. |
| C31 | 🟡 | Payment receipt emails link customers to the owner-only `/invoices/{id}` page → bounced to `/login`. | `webhooks/razorpay/route.ts:238,248` | Use `buildShareUrl('invoice', token)` like the reminders cron. |
| C32 | 🔵 | Payments page "This month" uses UTC month start (00:00–05:30 IST on the 1st counted in previous month) and only the last 200 payments. | `app/(app)/payments/page.tsx:31-56` | IST boundaries; aggregate in SQL. |
| C33 | 🔵 | Reports page: no `.limit` → PostgREST 1000-row cap silently truncates busy tenants; `new Date(issue_date) >= since` excludes the first day; day buckets are UTC. | `app/(app)/reports/page.tsx:37-52,200-206` | Aggregate server-side; IST. |
| C34 | 🔵 | Dashboard "unpaid invoices" hint counts drafts/cancelled (`balance_paise > 0` with no status filter); stat queries have no `limit` (1000-row cap). | `app/(app)/dashboard/page.tsx:25-27,92`, `invoices/page.tsx:47` | Filter status; count in SQL. |
| C35 | 🔵 | Timeline shows "Edited" for overdue marking and failed payments (`event: 'edited'`). | `cron/overdue/route.ts:50`, `webhooks/razorpay/route.ts:286`, `lib/events.ts:60` | Dedicated event types. |
| C36 | 🔵 | Razorpay orders under ₹1 fail (no minimum check; a 50-paise balance → 500 from Razorpay). | `payments/order/route.ts:85` | Guard `< 100 paise`. |
| C37 | 🔵 | `usage-reset` month arithmetic drifts (`setUTCMonth(+1)` from the 31st → Mar 3). | `cron/usage-reset/route.ts:38-42` | Use date-fns `addMonths`. |
| C38 | 🔵 | Admin "Apply allowances" dialog always initialises to 0 (current values not loaded) → clicking Apply resets a tenant's bonus limits to 0. | `components/admin/businesses.tsx:170-174,262-270` | Prefill current values. |
| C39 | 🔵 | `?days=abc` / `?page=abc` on admin APIs → 500 (`Number()` NaN → `new Date(NaN).toISOString()` throws). | `admin/stats/route.ts:19-20`, `reconcile/route.ts:29`, `businesses/route.ts:19` | Validate with zod. |
| C40 | 🔵 | Timeline `payment_recorded` and AI command diff rows hard-code INR, ignoring `doc.currency`. | `timeline.tsx:100`, `ai-command-bar.tsx:231,277` | Pass currency. |
| C41 | 🔵 | Email subject can read "Invoice INV-0001 from " when the business name is empty (route falls back to "Your supplier" for the body only). | `send-dialog.tsx:41-43` | Same fallback for subject. |
| C42 | 🔵 | Line-item `qty`/discount inputs snap to `0` when cleared (`Number('')` → 0 written back into a controlled input) → "05"-style values while retyping. | `line-items.tsx:178,205`, `document-editor.tsx:396-399` | Keep string state while editing. |
| C43 | 🔵 | Domains that receive mail via A-record fallback (no MX) are rejected as "cannot receive email" (RFC 5321 permits A fallback) — blocks some legitimate signups/customers/contact submissions. | `lib/validation/email-address.ts:154-160,268-300` | On `ENODATA`, try `resolve4/6`. |
| C44 | 🔵 | `AI_ENABLED` client value is baked in at build time; flipping `NEXT_PUBLIC_AI_ENABLED` at runtime makes buttons and routes disagree (contrary to the comment). | `lib/ai/enabled.ts:20` | Read server-side and pass down. |

---

## D. Auth & account flows

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| D1 | 🟠 | **Verification / reset links fail when opened in another browser/device or after the browser was closed — with no explanation.** Sign-up and `resetPasswordForEmail` use the server-side PKCE flow, which needs the `sb-…-code-verifier` cookie at `/auth/callback`; `asSessionCookie()` strips `maxAge`/`expires` from every Supabase cookie (also turning `maxAge: 0` deletions into "set to empty string"), so the verifier is a browser-session cookie. Desktop signup → open the email on the phone (or Gmail in-app browser) → `exchangeCodeForSession` fails → `/login?error=expired_link`. | `lib/supabase/session-cookie.ts:16-19`, `lib/supabase/server.ts:28`, `middleware.ts:41`, `app/(auth)/actions.ts:81-92,179-181`, `app/auth/callback/route.ts` | Use the `token_hash` + `verifyOtp` flow for email links; exempt the code-verifier cookie from `asSessionCookie`; don't strip `maxAge` when it is 0. |
| D2 | 🟠 | **`/login?error=…` is never rendered.** Callback redirects to `/login?error=missing_code|expired_link`; the login page reads only `next`. OAuth denials (`?error=access_denied`) also collapse into `missing_code`. User lands on a plain login page with no message. | `app/auth/callback/route.ts:20,27`, `app/(auth)/login/page.tsx:8-12` | Read `error` and show a banner; forward `error_description`. |
| D3 | 🟡 | Every server-action form wipes the user's input on error (React 19 resets uncontrolled `<form action>`): wrong password clears the email; a weak signup password clears all six fields while red errors point at empty inputs; same for contact and forgot-password. | `contact/contact-form.tsx:176-217`, `login/login-form.tsx:274-306`, `signup/signup-form.tsx:151-231`, `forgot-password/page.tsx:31-47` | Echo values in `FormState` → `defaultValue`, or controlled inputs. |
| D4 | 🟡 | `?error=quota_exceeded|convert_failed|duplicate_failed` on document pages is never displayed — duplicate/convert failures and quota exhaustion look like "nothing happened". | `app/(app)/actions.ts:245,271,332` → `quotations/[id]/page.tsx`, `invoices/[id]/page.tsx` | Read `searchParams.error`, render banner. |
| D5 | 🔵 | Sign in with Google/GitHub is offered on `/login` but not on `/signup` (the signup form even has a comment about "the OAuth block"). Verified live. | `app/(auth)/signup/signup-form.tsx` vs `login/login-form.tsx:266` | Add `OAuthButtons` to signup. |
| D6 | 🔵 | Signup "try again" link is a no-op (`/signup` → `/signup`; `useActionState` state persists so the success card never goes away). | `signup/signup-form.tsx:140` | Reset state / `router.refresh()`. |
| D7 | 🔵 | Reset-password copy promises "upper case, lower case and a number" but the schema only checks length ≥10 (signup does check complexity). Verified live. | `reset-password/page.tsx:87`, `lib/validation/schemas.ts:52-56` | Align. |
| D8 | 🔵 | `/reset-password` renders the form with no recovery session (only fails on submit) and, like `/forgot-password`, has no page `<title>` (falls back to the long default) because both are `'use client'` pages. Verified live. | `app/(auth)/reset-password/page.tsx:1`, `forgot-password/page.tsx:1` | Server page with `metadata` wrapping a client form; redirect when no session. |
| D9 | 🔵 | Root `error.tsx` / `not-found.tsx` send anonymous visitors to "Go to your dashboard" (→ login), and `error.tsx` says "drafts autosave every couple of seconds" on marketing pages. 404 page also has no specific `<title>`. Verified live. | `app/error.tsx:32-40`, `app/not-found.tsx:20-22` | Show dashboard link only when signed in; add title. |
| D10 | 🔵 | Sign-out cookie deletions become "set to empty" rather than deletes (side effect of D1's `asSessionCookie`). | `lib/supabase/session-cookie.ts` | Preserve `maxAge: 0`. |

---

## E. Demo booking, contact & support chat

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| E1 | 🟡 | Booking: DB insert result is ignored — a failed insert still returns "You're booked" and sends both emails. | `app/api/meetings/book/route.ts:100-110` | Check `{ error }` before sending mail. |
| E2 | 🟡 | Booking: double-booking race — slot verified then inserted with no unique constraint on `demo_bookings(starts_at) where status='confirmed'`. | `book/route.ts:73-76`, `0010_demo_meetings.sql` | Partial unique index; map 23505 → conflict. |
| E3 | 🟡 | Booking: a Google Calendar failure (expired refresh token, quota) 500s the whole booking — no row, no emails — despite the comment saying a bad integration must never lose the lead. | `book/route.ts:88-97` | try/catch `createMeetEvent`, continue with `meeting = null`. |
| E4 | 🟡 | Confirmation (UI + both emails) shows an IST time with no "IST" label to visitors who picked another timezone — a New York visitor who booked "3:00 PM" is confirmed for "…, 12:30 am". Confirm button also shows the IST calendar date next to a local-time slot ("Confirm 5:30 PM on 2026-08-16" for a slot that is 15 Aug local). | `book/route.ts:112-120`, `book-demo-flow.tsx:256,439`, `lib/email/templates.ts:391,402` | Format in the visitor's zone or append "IST". |
| E5 | 🟡 | Confirmation email unconditionally says "A calendar invite from Google is on its way separately" even when no calendar is connected. | `lib/email/templates.ts:391-399`, `book/route.ts:162` | Pass `inviteSent`. |
| E6 | 🟡 | Demo Meet-link reminder only fires if a cron run lands inside a ±15 min band (`now+105m … now+135m`); GitHub Actions schedules are routinely 10–40 min late → booking never picked up, and the Meet link is deliberately withheld from the confirmation email so the visitor never gets it. Bookings without `meet_link` are skipped forever. Comment still says "see vercel.json". | `app/api/cron/demo-reminders/route.ts:12-45`, `.github/workflows/demo-reminders.yml:9` | `starts_at > now() and <= now()+2h and reminder_sent_at is null`. |
| E7 | 🟡 | Non-IST visitors get a React hydration mismatch on `/book-demo` (`detectDefaultTimezone`/`todayIstIso` run during SSR as UTC→IST, then differ on the client) → full client re-render/flash. | `book-demo-flow.tsx:159-164` | Initialise timezone in `useEffect`. |
| E8 | 🟡 | Contact form reports success ("Thanks — we'll get back to you") when email is not configured — in production the message goes nowhere. Also the default inbox `support@invora.app` (when `CONTACT_EMAIL` unset) — see E9. | `app/(marketing)/contact/actions.ts:54,74-81` | Treat `email_not_configured` as failure in production. |
| E9 | 🟡 | Contact/support addresses are inconsistent and probably undeliverable: chat widget & footer use `vertexinfotech.team@gmail.com`; contact page `support@invora.app`; legal `legal@invora.app`; refunds `billing@invora.app`; privacy `privacy@invora.app`; contact-form and booking notifications default to `support@invora.app`. The site runs on `invorateam12.vercel.app` — if `invora.app` mailboxes don't exist, leads are lost. | `chat-widget.tsx:211`, `contact/page.tsx:56`, `legal-content.tsx:33`, `refunds/page.tsx:38,76`, `privacy/page.tsx:94`, `contact/actions.ts:54`, `book/route.ts:152` | One canonical, verified address; set `CONTACT_EMAIL`. |
| E10 | 🟡 | Contact/booking/quote-decision forms swallow field-level API errors — server puts the useful text (e.g. `"gmial.com" cannot receive email`) in `error.details`, the client only renders `error.message` ("Check the form."). | `book-demo-flow.tsx:238`, `components/public/quote-decision.tsx:53`, `book/route.ts:53,66`, `respond/route.ts:29` | Render `details.<field>`. |
| E11 | 🔵 | Chat widget: closed dialog remains in the tab order and readable by screen readers (`opacity-0 pointer-events-none` only; all 9 controls `tabIndex=0`, no `inert`/`aria-hidden`); no Escape to close; `h-4.5 w-4.5` is not a Tailwind class (icon renders 24px in a 36px circle). Verified live. | `components/marketing/chat-widget.tsx:112,156-167` | `inert` when closed; Escape handler; `h-[18px]`. |
| E12 | 🔵 | Timezone `<select>` has no accessible name (label contains only an icon); calendar day buttons lack full-date labels / `aria-pressed`. | `book-demo-flow.tsx:277-290,326-340` | Add `aria-label`s. |
| E13 | 🔵 | Honeypot response `{ok:true}` lacks `whenFormatted` → bots see "undefined on Google Meet." | `book/route.ts:58` | Return a fake `whenFormatted`. |
| E14 | 🔵 | Admin meetings page `minutesToLabel(1440)` renders end-of-day as "12:00 PM" ("12:00 AM – 12:00 PM"). | `app/(admin)/admin/(protected)/meetings/page.tsx:15-21` | Special-case 1440. |

---

## F. Marketing site, copy, SEO & metadata

| # | Sev | Finding | Where | Fix |
|---|---|---|---|---|
| F1 | 🟠 | Pricing comparison table says Free plan has **15** AI credits; cards, homepage, support-chat prompt and DB seed all say **7**. Verified live. | `app/(marketing)/pricing/page.tsx:23` vs `:63`, `page.tsx:136,529`, `0007_seed_plans.sql:14` | Change to 7. |
| F2 | 🟠 | Free-plan copy contradicts itself: pricing says "₹0 for your first 30 days… Auto-billing to Premium after your trial"; signup says "Free forever for 10 documents a month"; homepage "every month on the free plan"; refunds "Free plan is never charged"; settings/plan "Free forever". The code implements a permanent free plan (no trial mechanism). Verified live. | `pricing/page.tsx:10,58,68`, `signup/page.tsx:102`, `page.tsx:136`, `refunds/page.tsx:265`, `settings/plan/page.tsx:60` | Align all copy on the permanent free plan. |
| F3 | 🟠 | Footer "Vertex Infotech" links to `https://vertexinfotech.example` (reserved non-resolving TLD). Verified live. | `components/marketing/footer.tsx:17` | Real URL or drop link. |
| F4 | 🟡 | Open Graph image is wrong: `/logo.png` declared 1200×630 but is actually a **JPEG** (JFIF) at 1402×1122 with a `.png` extension; a correct 1200×630 `public/logo-full.png` exists unused. Cropped/rejected link previews. | `app/layout.tsx:47,54`, `public/logo.png` | Point OG/Twitter at `/logo-full.png`. |
| F5 | 🟡 | What happens on downgrade is described three different ways: Terms "never restricts access to documents already created"; Pricing "existing documents stay fully accessible"; Refunds + landing FAQ "documents beyond the Free allowance become read-only". | `terms/page.tsx:198-200`, `pricing/page.tsx:150-152`, `refunds/page.tsx:275-276`, `page.tsx:619` | Align (Terms is binding). |
| F6 | 🟡 | `/book-demo` missing from the sitemap. | `app/sitemap.ts:14-21` | Add. |
| F7 | 🔵 | Pricing FAQ "See our refunds and cancellation policy" is plain text, not a link. Verified live. | `pricing/page.tsx:174-176` | Link to `/refunds`. |
| F8 | 🔵 | Dark `themeColor` (`#1f1c19`) still advertised while the UI is forced light → dark browser chrome over a light page on mobile. | `app/layout.tsx:60-64`, `components/theme-provider.tsx` | Single light theme-color. |
| F9 | 🔵 | Privacy "processors" list incomplete: email may go via arbitrary SMTP (`SMTP_HOST`), demo bookings send visitor name/email/notes to Google Calendar — neither disclosed. | `privacy/page.tsx:65-68`, `lib/email/send.ts:59`, `lib/google/calendar.ts:122-138` | Update. |
| F10 | 🔵 | Reset/forgot pages, 404 page have no page-specific title (see D8/D9). Hero `<h1>` is server-rendered as the incomplete "From a one-line brief to a " (typewriter fills the rest client-side) — crawlers index a truncated H1. | `components/marketing/typing-text.tsx`, `page.tsx` | Render the first phrase in SSR. |
| F11 | ⚪ | FAQ `<summary>` reads "…? +" to screen readers (`+` span needs `aria-hidden`). Landing/nav logo `<img alt="">` on the brand mark. | `page.tsx:647-649`, `components/marketing/nav.tsx` | `aria-hidden`; alt "Invora". |
| F12 | ⚪ | Landing footer/legal pages brand the product as `invora.app` throughout while the deployment is `invorateam12.vercel.app` (see E9). | various | Decide domain. |
| F13 | ⚪ | Public share pages for an invalid/expired token return HTTP 200 with a "no longer available" body instead of 404/410 (`noindex` is set, so low impact). Verified live. | `app/q/[token]/page.tsx`, `app/i/[token]/page.tsx` | `notFound()` / status 410. |
| F14 | ⚪ | Root `logo.png`, `package.json`, `package-lock.json` are modified but uncommitted in the working tree. | `git status` | Commit or discard. |

---

## G. In-app UI/UX (smaller)

| # | Sev | Finding | Where |
|---|---|---|---|
| G1 | 🟡 | "New quotation / New invoice" from a customer page doesn't preselect the customer (`?customer=` ignored). | `customers/[id]/page.tsx:72,78` → `quotations/new/page.tsx:14-19`, `invoices/new/page.tsx` |
| G2 | 🟡 | `<Link>` to `/api/pdf/...?download=1` in every list row and on the public document page → Next.js prefetches in-viewport links, rendering PDFs per page view and burning the 30/min `pdf` limit; click goes through the client router then hard-navigates. | `components/documents/document-list.tsx:77-83`, `components/public/document-view.tsx:213-218` — use `<a>` |
| G3 | 🟡 | Pay panel gets stuck on "Confirming…" after the 60 s poll or after a partial payment (`status` never becomes `paid`); "Pay now" stays disabled with no explanation if `checkout.js` is blocked (ad-blocker), `onError` unhandled. | `components/public/pay-panel.tsx:250-264,346-370` |
| G4 | 🔵 | Validation errors for many fields are never rendered (customer: country/address/city/state/postal/notes; product: unit/price/tax_rate/discount/sku/hsn) → "Please fix the highlighted fields" with nothing highlighted; e.g. clearing Country fails `length(2)`. | `components/app/customer-form.tsx`, `components/app/product-form.tsx` |
| G5 | 🔵 | Country input `className="uppercase"` is cosmetic; stored as typed (`in`). | `lib/validation/schemas.ts:76,124` |
| G6 | 🔵 | List search placeholder promises "number or customer" but only `number` is filtered. | `document-list.tsx:148` vs `quotations/page.tsx:40`, `invoices/page.tsx:42` |
| G7 | 🔵 | Invoice list filter has no "Viewed"/"Cancelled" chips; customer search drops the `archived` filter and vice-versa; empty archived list says "No customers yet / Add your first customer". | `invoices/page.tsx:16-23`, `customers/page.tsx:57-91` |
| G8 | 🔵 | After saving a customer/product there is no success feedback and the customer page resets to the Activity tab. | `app/(app)/actions.ts:92-93,148-149`, `customers/[id]/page.tsx:109` |
| G9 | 🔵 | Catalog items can never be archived/removed (`products.archived_at` exists, nothing sets it); archived customers can never be un-archived. | `products/[id]/page.tsx`, `customers/[id]/page.tsx:83-90` |
| G10 | 🔵 | Mobile line-item cards omit Disc % and the catalog picker (a desktop-entered discount is invisible on mobile); desktop "Reorder handle" column has no drag behaviour. | `components/documents/line-items.tsx:123,138,250-332` |
| G11 | 🔵 | Share dialog cannot revoke without minting a new link (revoke button only appears after `url` is set). | `components/documents/share-dialog.tsx:141-186` |
| G12 | 🔵 | Choosing the same CSV again after "Choose another file" does nothing (input value never reset). | `components/app/csv-importer.tsx:57-87,165` |
| G13 | 🔵 | Premium subscriber can't cancel from the UI (coming-soon check runs before `!isFree`) → admin-activated premium user sees disabled "Coming soon" instead of "Cancel subscription". Upgrade button stays disabled after client-side nav back (`next/script` `onLoad` fires once; use `onReady`). | `components/app/upgrade-button.tsx:110-139`, `settings/plan/page.tsx:73` |
| G14 | ⚪ | `aria-current="true"` on filter chips (should be `"page"`); `RewriteButton` dialog lacks `DialogDescription`. | `document-list.tsx:162`, `rewrite-button.tsx:104-108` |

---

## H. Ops, docs & tests

| # | Sev | Finding | Where |
|---|---|---|---|
| H1 | 🔵 | `docs/ENV.md` drift — undocumented: `SMTP_HOST/PORT/USER/PASS`, `NEXT_PUBLIC_AI_ENABLED`, `SEED_USER_EMAIL`; documented but unused: `NEXT_PUBLIC_APP_NAME`, `SENTRY_AUTH_TOKEN`; webhook event list omits `subscription.pending/resumed`; "EMAIL_REPLY_TO falls back to the business's own email" is false. | `docs/ENV.md`, `lib/email/send.ts` |
| H2 | 🔵 | Stale comments: `usage-reset/route.ts:11` "hourly"; `demo-reminders/route.ts:15` "see vercel.json (*/15)"; `0008_admin_views.sql:62` claims views inherit RLS. | |
| H3 | 🔵 | Tests: `tests/integration/webhook.test.ts:238` titled "returns 500 when not valid JSON" but asserts 400 (400 is right, title wrong); header promises a "genuine processing failure returns 5xx and is retried" test that doesn't exist (would expose C5). `tests/e2e/core-flow.spec.ts:29-30` expects heading `/^Premium$/` but pricing headings are "Premium — Monthly/Yearly" → e2e fails. `rls.test.ts` doesn't cover the `v_*` views, `usage_counters`, or any RPC with a foreign `business_id` (exactly where B1/B2 live). | |
| H4 | 🔵 | No migration contains a single `GRANT`/`REVOKE` beyond the anon revoke on views — B1/B2 are reachable precisely because everything in `public` is auto-exposed. | `supabase/migrations/*` |

---

## Suggested fix order

1. **Today:** set `UPSTASH_REDIS_REST_URL/TOKEN`, `NEXT_PUBLIC_APP_URL`, `CRON_SECRET`, `RAZORPAY_WEBHOOK_SECRET`, `CONTACT_EMAIL` in Vercel (A1–A4, E9). Redeploy. This alone unblocks signup, booking, contact, chat, share links, PDFs, AI and payments.
2. **This week (security):** B1 (quota RPC guards + revoke), B2 (`security_invoker` on views), B3 (`suspended_at` lock), B4/B5 (status guards on save/respond).
3. **This week (money):** C1 (`lpad` truncation), C2 (PDF fonts), C3/C4 (autosave in-flight guard + transactional item replace), C5 (webhook retry), C6/C7 (period roll).
4. **Then:** D1/D2 (auth links + error banner), booking robustness (E1–E7), copy alignment (F1–F5), and the medium list.
